# 008-mentors — Mentors — Changelog

> Append-only. Never edit or delete existing entries.

## 2026-04-25
- Initiative directory created (stub)
- Plan, status, and changelog files created as drafts pending `/brainstorm` + `/plan`
- Plan filled in: `Mentor` model with MANUAL/CSV source, admin CRUD, CSV importer with partial-success row counting, `MentorService.is_mentor` consumed by intake. Clarified: "CARGAS" on the whiteboard is the CSV importer here, not a separate microservice. Replaces 004's `FalseMentorService`.
- Worktree set up at `../solicitudes-008-mentors` on branch `feat/008-mentors` (off main `2b4d362`); isolated docker stack `docker-compose.worktree.yml` (project `solicitudes-w008`) so it runs in parallel with main 004 stack.
- Merge-back ordering note added to status.md: Cross-app wiring + End-to-end smoke + E2E sections all defer until 004 lands and this branch rebases.
- §1 App skeleton: `app/mentores/__init__.py` + `apps.py` (`MentoresConfig`, verbose_name "Catálogo de mentores"); registered in `config.settings.base.INSTALLED_APPS`; added to coverage source. Verified via `manage.py check` — no issues.
- §2 Model & migration: `MentorSource` StrEnum (MANUAL/CSV) + `DEFAULT_MATRICULA_REGEX` in `mentores/constants.py`; `Mentor` model in `mentores/models/mentor.py` (matricula PK, activo, fuente, nota, fecha_alta/fecha_baja, creado_por FK to AUTH_USER_MODEL on_delete=PROTECT, indexed on activo); migration `0001_initial` generated and applied cleanly against the worktree postgres.
- §3 Schemas + exceptions: `MentorDTO` (frozen output), `MentorUpsertInput` (repo boundary), `CsvImportResult` (frozen, with `invalid_rows` list of dicts) in `schemas.py`; `MentorNotFound`/`MentorAlreadyActive`/`CsvParseError` in `exceptions.py` subclassing `_shared.exceptions` `NotFound`/`Conflict`/`DomainValidationError`. Spanish `user_message` per project convention.
- §4 Repository: `MentorRepository` ABC in `repositories/mentor/interface.py` with `get_by_matricula`, `exists_active` (hot path, primitive return), `list(only_active, page)`, `upsert -> (DTO, UpsertOutcome)`, `deactivate`. `OrmMentorRepository` in `implementation.py` (transaction.atomic + select_for_update on writes; reactivation rewrites `fecha_alta`/clears `fecha_baja`; Mentor.DoesNotExist mapped to MentorNotFound). 14/14 tests green: hot path, upsert outcomes (INSERTED/REACTIVATED/ALREADY_ACTIVE), deactivate idempotency + missing, list filtering + pagination.
- §5 Services: `MentorService` ABC + `DefaultMentorService` (validates matricula format via `validators.is_valid_matricula`; raises `MentorAlreadyActive` on active duplicate; reactivation is silent on success). `MentorCsvImporter` ABC + `DefaultMentorCsvImporter` (UTF-8 BOM-tolerant decode, header check, row-level validation that does not abort the batch, full atomic for DB-level rollback). `dependencies.py` factories for repo/service/importer. `InMemoryMentorRepository` fake in `tests/fakes.py` for service-layer tests. `mentores/validators.py` centralizes matricula format check (configurable via `MENTOR_MATRICULA_REGEX`, default `^\d{8}$`). 34/34 tests green: 14 repository + 9 service + 11 csv importer.
- §6 Forms, views, URLs, templates: `AddMentorForm` + `CsvImportForm`; four CBVs (`MentorListView`, `AddMentorView`, `DeactivateMentorView`, `ImportCsvView`) all gated by `AdminRequiredMixin`; URL module mounted at `/mentores/` from `config/urls.py`; 5 templates extending `base.html` (list with pagination + empty state, add, confirm_deactivate, import_csv, import_result with neutral count tiles). 16/16 view tests green via the JWT-cookie pattern. Visually verified at 1280x900 desktop and 320x800 mobile against the worktree dev stack — sidebar collapses to hamburger, low-priority columns drop, no horizontal scroll, single primary action per page, status badges carry text + color. Bug-fix in shared `templates/components/empty_state.html` — multi-line `{# … #}` rewritten as `{% comment %}…{% endcomment %}` (Django strips only single-line `{# … #}`).
- §7 Quality gates: `ruff check mentores/` clean; `mypy mentores/` clean (37 source files); full pytest 272/272 in-process tests passing (Tier 2 Playwright tests skipped — chromium not installed in worktree container, preexisting infra issue, unrelated to this initiative). `mentores/` coverage 98%: services 100%, repository 100%, views 88–97%, all above plan targets.
- Code-review pass (code-reviewer agent, fresh context): assessment "Ready to proceed (sections §1–§7)", no Critical issues. Applied review fixes: (1) replaced `datetime.now(tz=UTC)` with `django.utils.timezone.now()` in `repositories/mentor/implementation.py` and `tests/fakes.py` for freezegun consistency with the rest of the codebase; (2) removed unused `BytesIO` import + `del` line in `tests/test_views.py`; (3) added comment in `services/mentor_service/implementation.py` documenting why format validation is duplicated between form and service (CSV importer bypasses forms). Pushed back on reviewer's claim that anonymous list view returns 401 — verified in `_shared/middleware/error_handler.py:77` that HTML clients are redirected (302), only JSON gets 401; tightened the test to assert 302 with a comment explaining the contract. Re-verified: 50/50 mentores tests pass, ruff + mypy still clean.
- §8 paused intentionally — Cross-app wiring + End-to-end smoke + E2E sections defer until 004 lands on main and this branch rebases. See "Merge-back ordering (worktree note)" in status.md for the order of operations.
- 004 shipped (`9b923b3`). Worktree retired: stashed all 008 work onto `main` as pending changes (zero conflicts — 004 didn't touch any of the 6 files I'd modified), then removed `feat/008-mentors` worktree, deleted the empty branch, dropped `docker-compose.worktree.yml`, and tore down `solicitudes-w008` containers + volume. All work continues on `main` as uncommitted changes for user review.
- §"Cross-app wiring": new adapter `solicitudes/intake/adapters/mentores_adapter.py` (`MentoresIntakeAdapter`) that implements 004's outbound port `solicitudes.intake.mentor_port.MentorService` by delegating `is_mentor` to the richer `mentores.services.mentor_service.MentorService` — keeps each feature's interface independently evolvable per the project's hexagonal/ports-and-adapters style. `solicitudes/intake/dependencies.py` now wires the real adapter; `FalseMentorService` left in `mentor_port.py` as a doc-only fallback.
- §"End-to-end smoke" + §"E2E" Tier 1: new test module `mentores/tests/test_intake_wiring.py` with 7 cross-feature integration tests using Django Client through real JWT cookies — covers all three Tier 1 cross-feature scenarios (mentor + mentor_exempt → no comprobante & pago_exento=True; non-mentor + mentor_exempt without comprobante → 400; deactivation snapshot integrity), the three smoke scenarios (100-row CSV with 80 inserted / 5 skipped / 15 invalid; manual duplicate add → 409 with Spanish copy; deactivated row reactivated via CSV), and an extra "comprobante attached" success-path case. 7/7 green.
- §"E2E" Tier 2: new `tests-e2e/test_mentores_golden_path.py` with two browser tests via `pytest-playwright` + `live_server` — admin imports a CSV through the upload form (verifies result page + populated catalog) and admin deactivates a mentor from the list view (verifies confirmation page + post-deactivation list state with both filter modes). Captures screenshots at 1280x900 and 320x800 to `/tmp/screenshots-008/` per the frontend-design skill. The deactivate test surfaced a real UX bug: unchecked checkbox doesn't send a key, so my view couldn't tell "fresh load" from "filter applied with checkbox off" — fixed by adding a hidden `filtered=1` sentinel to the filter form and updating `mentores/views/list.py` to gate the only-active default on it. 2/2 green.
- Final quality gates on main with all 008 + cross-feature changes: `ruff check mentores/ solicitudes/intake/` clean; `mypy mentores/ solicitudes/intake/` clean (63 source files); full `pytest` 361/362 passing — the 1 failure is a pre-existing flaky `tests-e2e/test_tipos_golden_path.py::test_admin_creates_lists_and_edits_a_tipo` (verified by stashing all my changes and re-running — same failure on clean main; unrelated to 008).
- Implementation status: complete, pending user review on `main`. All sections of `status.md` checked off. Roadmap update to "Done" deliberately left for the user (per their SDD initiative-closeout convention: closeout happens after their `/review`, not auto-flipped here).
