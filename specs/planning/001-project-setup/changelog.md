# 001-project-setup — Project Setup & Base — Changelog

> Append-only. Never edit or delete existing entries.

## 2026-04-25
- Initiative directory created (stub)
- Plan, status, and changelog files created as drafts pending `/brainstorm` + `/plan`
- Plan filled in: layout, settings split, `_shared/` infra (exceptions, middleware, auth helpers, pagination, pdf wrapper), base templates, quality gates. Status checklist populated. Open questions: static serving in prod, X-Request-ID trust policy.
- **Docker folded into 001** (per user direction): multi-stage `Dockerfile` with WeasyPrint OS deps baked in, `docker-compose.dev.yml` (web + db + mailhog, hot reload, healthchecks), `docker-compose.test.yml` (Postgres-only, tmpfs, no app container — daily test loop stays in-process via `pytest-django`'s `live_server`), `config/settings/test_postgres.py` for opt-in real-DB tests, `Makefile` targets (`up/down/test/e2e/e2e-postgres/e2e-headed/lint/type`). Followed `.claude/skills/django-patterns/e2e.md` rules: dev compose forbidden as test target; test compose has no `web` service; SQLite remains the default test backend.
- **Everything-through-Docker tightening:** Makefile now proxies every Python command (`pytest`, `ruff`, `mypy`, `manage.py …`) via `$(DC_DEV) exec -T web`. Host machines need only Docker — no Python install required. Test compose joins `solicitudes-net` as an `external` network; `postgres-test` is reachable from inside `web` via Docker DNS at `postgres-test:5432` (no host port published). `test_postgres.py` updated: `HOST=postgres-test`, `PORT=5432`. IDE remote-interpreter setup documented in `tests-e2e/README.md`. Acceptance criterion: `make test` passes on a host with no local Python.
- **Source code wrapped under `app/` + flat per-app layout:** The git repo root holds infra only (Dockerfile, compose, Makefile, env). All Python/Django source lives inside `app/`. Each Django app sits **directly** under `app/` (no `apps/` namespace): `app/_shared/`, `app/usuarios/`, `app/solicitudes/`, `app/notificaciones/`, `app/mentores/`, `app/reportes/`. `INSTALLED_APPS` and imports use bare names (`"_shared"`, `from usuarios.services... import ...`). Dockerfile `COPY app/ /app/` (subdir-scoped); dev compose bind mount `./app:/app`. Cascaded across all planning docs, rules, skills, CLAUDE.md, and architecture.md via bulk sed (specs/apps/ cross-references protected).
- **Nginx + TLS in dev too** (per user direction): added `nginx/dev/nginx.conf` (permissive, TLSv1.2+1.3, `/__mailhog/` vhost) and `nginx/prod/nginx.conf` (hardened: TLSv1.3 only, HSTS, CSP, rate limiting). Added `nginx-dev` service to `docker-compose.dev.yml`; dropped host port publishing from `web` and `mailhog` (only `nginx-dev` exposes 443/80, plus `db` keeps 5432 for psql tooling). All browser traffic now goes through `https://localhost` — **no `http://...:port` URLs anywhere in docs**. Mailhog UI moved from `localhost:8025` to `https://localhost/__mailhog/`. Self-signed certs via `make certs` (mkcert preferred, openssl fallback). `SITE_BASE_URL` is `https://localhost` in dev. `SECURE_PROXY_SSL_HEADER = ("HTTP_X_FORWARDED_PROTO", "https")` so Django sees requests as secure. `certs/`, `*.crt`, `*.key` added to `.gitignore`. Tests bypass nginx (`live_server` is in-process, per `e2e.md`).

## 2026-04-25 — Implementation pass

- **Bootstrap files written:** `app/pyproject.toml` (ruff + mypy + pytest config; `N818` and `UP046` ignored intentionally), `app/requirements.txt`/`requirements-dev.txt` (Django 5.1, pydantic 2.13, psycopg 3, PyJWT 2.12, weasyprint 68, gunicorn, pytest stack), `app/manage.py`, `.env.example`, `.dockerignore`, expanded `.gitignore`.
- **`config/` package:** settings split (`base.py`, `dev.py`, `prod.py`, `test_postgres.py`); `urls.py` with `/admin/`, `/health/`, `/` → `/solicitudes/` redirect; `wsgi.py`, `asgi.py`. `prod.py` enforces required env vars (`_required` helper). `dev.py` flips to Postgres only when `DB_HOST` is present (SQLite fallback otherwise).
- **`_shared/` infrastructure:** `apps.py` (`SharedConfig`, label `shared`); `exceptions.py` (`AppError` + 6 sentinels matching the planned codes/HTTP statuses); `pagination.py` (`PageRequest` + `Page[T]` with `total_pages`/`has_next`/`has_prev` computed fields); `auth.py` (`decode_jwt`, `parse_claims`, `JwtClaims`); `pdf.py` (lazy WeasyPrint import); `logging_config.py` (`build_logging_config`, `RequestIDFilter`, `set_request_id`/`reset_request_id`/`get_request_id` via `contextvars`); `views.py` (`health` endpoint).
- **Middleware:** `request_id.py` (mints uuid4 hex; accepts a sanitized incoming `X-Request-ID` matching `^[A-Za-z0-9._\-]{8,128}$`; resets contextvar in `finally`); `logging.py` (`request.start` debug + `request.end` info with method/path/status/duration_ms); `error_handler.py` (HTML render via `_shared/error.html`, JSON branch on `HX-Request` or `Accept: application/json`, redirect to `LOGIN_REDIRECT_URL` for `AuthenticationRequired` HTML clients, `field_errors` in JSON for `DomainValidationError`).
- **Templates & static:** `base.html` (Bootstrap 5, vendored, accessibility blocks); `components/{nav,alerts,pagination,empty_state}.html`; `_shared/{error,404}.html`; `static/css/app.css` with UAZ palette. Bootstrap 5 vendor files left as stubs with a README pointing at the cdn.jsdelivr URLs to drop in before shipping UI.
- **Docker:** multi-stage `Dockerfile` (builder installs WeasyPrint + libpq build deps; runtime ships only runtime libs + non-root `app` user); image built and verified WeasyPrint smoke produces `b"%PDF"` bytes inside the runtime image. `docker-compose.dev.yml` (`nginx-dev` 80/443 + `web` 8000 internal + `db` 5432 + `mailhog` internal); `docker-compose.test.yml` (`postgres-test` only, tmpfs, joins `solicitudes_solicitudes-net` as `external`); `Makefile` (`up/down/build/logs/shell/migrate/makemigrations/lint/type/test/e2e/e2e-postgres/e2e-headed/clean/help/certs`, all Python via `$(EXEC)`).
- **Verification (all run inside the `web` container, host has no Python):**
  - `docker build --target runtime` succeeds; image size **731 MB** (< 800 MB target).
  - WeasyPrint smoke inside image: 2.2 KB PDF output starting with `%PDF`.
  - `make certs` (openssl fallback) populates `certs/server.crt` + `certs/server.key`.
  - `docker compose up`: nginx-dev healthy, web healthy, db healthy, mailhog up.
  - `https://localhost/health/` → 200, JSON `{"status":"ok","request_id":"…"}`.
  - `http://localhost/health/` → 301 to `https://localhost/health/`.
  - `https://localhost/__mailhog/` → 200, Mailhog UI served.
  - `X-Request-ID` from nginx (`$request_id`) reaches Django and round-trips back in the response header.
  - `make migrate` against the dev `db` succeeds (Django auth/sessions migrations applied).
  - `pytest`: **35 passed**, `_shared` coverage **98%** (target ≥ 90%); zero warnings, zero skipped.
  - `pytest --ds=config.settings.test_postgres` against the live `postgres-test` service on the shared `solicitudes-net`: **35 passed**.
  - `docker compose -f docker-compose.test.yml down -v` removes the test volume cleanly (no leftover `postgres-test` volume in `docker volume ls`).
  - `web` container runs as `uid=1000(app) gid=1000(app)`.
  - `ruff check .` clean (after configuring `N818`/`UP046` ignores for the intentional naming and Generic[T] choices).
- **Deferred (out of bootstrap critical path, not blockers):** real Bootstrap 5 vendor assets (stubs in place); `make type` (strict mypy + django-stubs needs its own tuning pass); `manage.py check` against `prod.py` (needs real prod env vars); frontend baseline review (meaningful only once a real UI initiative lands).

- **Code review pass (`code-reviewer` agent, applied via `/receive-review`):**
  - **Critical — fixed:** dropped the deprecated `default_app_config` from `_shared/__init__.py` (Django 5 ignores it); removed `SharedConfig.label = "shared"` so the app label matches the `_shared` import path used everywhere else.
  - **Important — fixed:** dropped `django.contrib.admin` from `INSTALLED_APPS` and the `/admin/` URL — RNF-01 (external auth) means there are no `is_staff` users to log in; consolidated `LOGGING` so it's only set in leaf settings (`dev.py`, `prod.py`) and reads `LOG_LEVEL` from env; trimmed `[tool.coverage.run].source` to `["_shared", "config"]` (apps will be added as they ship); switched `test_pagination.py` to `pytest.raises(ValidationError)` instead of bare `Exception`; pinned `python-json-logger>=2.0,<3` (3.x renamed the formatter import path); stopped surfacing the JWT library's exception message in `AuthenticationRequired` — it's now logged server-side instead.
  - **Important — implemented missing prod branch:** `AppErrorMiddleware.process_exception` now catches non-`AppError` exceptions when `DEBUG is False`, logs `error.unhandled` with `exc_info=True` + `request_id`, and renders `_shared/error.html` with `code="internal_error"` (or JSON if requested). Two new tests using `@override_settings(DEBUG=False)` lock that behavior in. Plan acceptance criterion (plan.md:663) now satisfied.
  - **Suggestion — applied:** none of the cosmetic suggestions changed; `RequestIDMiddleware` regex test for the 32-char hex round-trip already covered by the nginx end-to-end check.
  - **Plan drift to fix at closeout (not in this initiative):** plan.md:38 references `tests-e2e/auth/*.json` without the `app/` prefix; plan.md:255 still mentions `source = ["apps", "config"]` (legacy layout). Both are documentation-only.
  - Re-verified: `pytest` 37 passed, `_shared` coverage 98%; `ruff check .` clean; `manage.py check` clean.

- **`make type` brought online:** ran `mypy .` strict against the runtime image; surfaced 3 errors (an unused `type: ignore` in `_shared/pdf.py`, `Any`-return from `HTML.write_pdf()`, and an `attr-defined` on `LogRecord.request_id` in the request-id filter test). Fixed by typing `pdf` explicitly in the WeasyPrint wrapper, dropping the obsolete `type: ignore`, and tagging the test attribute access with `# type: ignore[attr-defined]`. Result: `Success: no issues found in 30 source files`. Status item flipped from deferred to done.
