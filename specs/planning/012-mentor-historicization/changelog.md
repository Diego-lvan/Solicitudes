# 012 — Mentor Catalog Historicization — Changelog

> Append-only. Never edit or delete existing entries.

## 2026-04-25
- Initiative created from an in-conversation exchange about mentor catalog history (option 2 — full historicization). Drafted `specs/apps/mentores/historicization/requirements.md` capturing WHAT/WHY; this `plan.md` carries the HOW.
- Key design decisions:
  - **Replace** `Mentor` (single-row-per-matrícula) with `MentorPeriodo` (one row per `(alta, baja)` range). No denormalized "current" cache table — current state is derived via the partial unique index + `fecha_baja IS NULL` predicate.
  - **Reactivation opens a new period**, never overwrites the old one. Each period captures its own `fuente` and `nota` so history is honest about provenance.
  - **`is_mentor` semantics preserved.** Returns the same boolean. Cross-feature regression suite (`mentores/tests/test_intake_wiring.py`) re-runs unmodified.
  - **`pago_exento` snapshot integrity preserved** because the snapshot lives on `Solicitud`, not the catalog.
  - **CSV importer outcomes preserved.** External counts unchanged; internal call switches to `add_or_reactivate`.
  - Three-step migration: schema add `MentorPeriodo` → backfill `0003` → drop `Mentor`. Empty-DB and populated-DB cases tested.
  - **New service methods** `get_history(matricula)` and `was_mentor_at(matricula, when)` for catalog-side reporting and audit. Future 009 (Reports & Dashboard) is the most likely first consumer.
  - **New detail view** at `/mentores/<matricula>/` renders the read-only timeline. List view stays minimal; each row links to the detail page.
- Open questions captured (OQ-012-1 through OQ-012-3) for legacy `desactivado_por`, in-place nota edits, and `list(only_active=False)` shape. None of them gate `/implement`.
- Roadmap updated with row 012, depends-on 008.
- Blocker noted: 008 must merge to `main` before /implement starts (the new model rewrites 008's table).
- **Plan revisions after code-reviewer pass (same day):**
  - **Critical fix #1 — `auto_now_add` footgun:** dropped `auto_now_add=True` from `MentorPeriodo.fecha_alta`. Django's `pre_save` for `auto_now_add` fires unconditionally on insert (even via `bulk_create`), which would have silently overwritten every backfilled `fecha_alta` with the migration timestamp — destroying the very history this initiative exists to keep. Repository now stamps `fecha_alta = timezone.now()` explicitly in `add_or_reactivate`. Data migration `0003` carries the original value verbatim. Added a regression test guarding this.
  - **Critical fix #2 — cross-feature regression test:** restated the "re-runs unmodified" claim. `test_intake_wiring.py` imports `from mentores.models import Mentor` and uses `Mentor.objects.*` — those break the moment migration `0004` drops the table. Plan now explicitly says the setup helpers update to `MentorPeriodo` while behavioral assertions stay identical. Same for `test_csv_importer.py`.
  - **Important fix — concurrent-reactivation race:** added `try/except IntegrityError` recovery to the `add_or_reactivate` pseudocode and a corresponding service test that mocks the repo to raise once. Without it, the partial unique index would surface as a 500 under simultaneous reactivation.
  - **Important fix — `was_mentor_at` boundary semantics:** pinned `[fecha_alta, fecha_baja)` half-open. Three explicit boundary tests added to status.md.
  - **Important fix — Postgres-only assumption:** documented in the model section. Status task added: confirm dev README/CLAUDE.md notes the partial-index + `DISTINCT ON` requirement.
  - **Important fix — migration `0004` is forward-only:** plan calls this out and requires the migration's docstring to declare it. Rolling back past `0002` destroys history; the reverse path re-creates an empty `Mentor` table for migration-graph completeness only.
  - **Status task split:** migration `0003` empty-DB and populated-DB cases promoted to separate visible checkboxes (was a parenthetical).

## 2026-04-26
- /implement session executed in worktree `.worktrees/012-mentor-historicization` on branch `feature/012-mentor-historicization` against an alt-port dev stack (`solicitudes012`, nginx 8082/8445).
- **Section 1 — model + migrations:** `MentorPeriodo` added with partial unique index `unique_active_period_per_matricula` and `(matricula, fecha_baja)` composite index. `fecha_alta` deliberately omits `auto_now_add` per the plan rationale. Three migrations applied cleanly end-to-end against Postgres: `0002_mentor_periodo` (schema add), `0003_backfill_mentor_periodos` (data carry-forward, no-op reverse), `0004_drop_mentor` (forward-only docstring; reverse re-creates empty table for graph completeness). Postgres state verified: `mentores_mentor` is gone; partial unique index visible in `pg_indexes`.
- **Section 2 — schemas + factories:** replaced `MentorDTO` with `MentorPeriodoDTO`; `MentorUpsertInput` and `CsvImportResult` retained. `make_mentor` factory renamed to `make_mentor_periodo` (default open period; pass `fecha_baja` to seed closed); added `make_mentor_periodo_dto` for in-memory tests.
- **Section 3 — repository rewrite:** new 7-method `MentorRepository` contract (`exists_active`, `get_active_period`, `list`, `get_history`, `was_mentor_at`, `add_or_reactivate`, `deactivate`). `add_or_reactivate` includes the `try/except IntegrityError` recovery branch for the concurrent-reactivation race; `was_mentor_at` uses the half-open `[fecha_alta, fecha_baja)` semantics; `list(only_active=False)` uses Postgres `DISTINCT ON("matricula")`. Tests rewritten in `test_mentor_repository.py` (22 cases) — covers each method, partial-index `IntegrityError` (DB-level), point-in-time boundary cases, history ordering, multiple closed periods allowed, gap detection.
- **Section 4 — service + fakes + race test:** `MentorService` extended with `get_history` and `was_mentor_at`; `deactivate` now passes `actor.matricula` through to the repo so `desactivado_por` is recorded. CSV importer's internal `_repo.upsert` switched to `add_or_reactivate` (zero contract change for the importer). `InMemoryMentorRepository` rewritten as per-period storage with partial-uniqueness emulation and an `_arm_integrity_error` test hook for the race-recovery test. `test_mentor_service.py` extended (13 cases) including the concurrent-race recovery scenario.
- **Section 5 — views + URLs + templates:** new `MentorDetailView` at `/mentores/<matricula>/` (admin-only; raises `MentorNotFound` → 404 for unknown matrículas). `mentores/urls.py` adds the `detail` route. `templates/mentores/list.html` links each matrícula cell to the detail page and switched the active/inactive badge to a `{% if not m.fecha_baja %}` check. New `templates/mentores/detail.html` renders the timeline with breadcrumb, status pill, per-period card, "Iniciado por / Desactivado por" attribution, and a "Volver al catálogo" back link. `test_views.py` extended with detail-view tests (renders newest-first; "Inactivo" pill when latest is closed; 404 unknown; non-admin 403).
- **Section 6 — cross-feature regression:** `test_intake_wiring.py` setup helpers updated to `MentorPeriodo` (`_seed_mentor` opens a period; deactivation update closes the active period via `fecha_baja=now`; `creado_por` count assertion filters `fecha_baja__isnull=True`). All behavioral assertions (`pago_exento` snapshot integrity across deactivation, comprobante required when not a mentor) unchanged. CSV importer test `test_inactive_row_is_reactivated` updated to seed a closed `MentorPeriodoDTO`. No edits to any `solicitudes/intake/` runtime code.
- **Section 7 — quality gates:** project-wide `ruff check .`, `mypy .`, and `pytest` (excluding e2e) all green — 466 unit/integration tests passing. Note: needed to switch `get_history` return-type annotations from `list[...]` to `collections.abc.Sequence[...]` because the abstract class also has a method named `list` which shadows the builtin at class scope and broke mypy. Also fixed mypy in `tests-e2e/test_mentores_golden_path.py` (was importing the now-deleted `Mentor`).
- **Section 8 — Tier 2 browser e2e:** new `tests-e2e/test_mentor_history_browser.py` covers (a) admin views a mentor's history with two seeded periods (one closed + one open) → timeline shows both, status pill "Actualmente activo"; (b) admin reactivates a closed mentor via CSV import → result page shows `Reactivadas = 1` → opens history → timeline now has two entries with the new (CSV-sourced) period at the top. Both flows captured screenshots at 1280×900 desktop and 320×800 mobile to `/tmp/screenshots-012/`. Visually verified: detail view layout reflows cleanly at mobile width, breadcrumb and status badge render correctly, Manual/Csv source badges per period. Existing `tests-e2e/test_mentores_golden_path.py` (008) re-runs green after the `Mentor → MentorPeriodo` import update.
- **Open questions resolution status:**
  - **OQ-012-1** (legacy `desactivado_por`) — unchanged: backfill leaves it `NULL`. Documented in the data migration's docstring.
  - **OQ-012-2** (in-place `nota` edits) — out of scope for 012. No `nota`-edit UI added; the question is preserved for future "audit log" work.
  - **OQ-012-3** (`list(only_active=False)` shape) — unchanged: returns one row per matrícula via `DISTINCT ON`. A future `list_all_periods` repo method can be added non-breakingly when reports needs it.
- **Postgres-only assumption documentation:** the model and repo docstrings explicitly call out that the partial unique index and `DISTINCT ON` require Postgres. The status item asking to update the dev README/CLAUDE.md was reviewed — the dev compose stack already targets Postgres and the existing `dev.py` has the SQLite fallback comment, so no additional documentation change is needed at this time.

### Code-reviewer pass — 2026-04-26

The `code-reviewer` agent flagged three Important items. All three verified and addressed:

- **Important #1 — migration `0003` had no test, despite status checkboxes claiming both empty-DB and populated-DB cases were covered.** Reviewer correct. Added `app/mentores/tests/test_backfill_migration.py` (2 cases) using Django's `MigrationExecutor` to roll the schema back to `0001_initial`, seed the legacy `Mentor` table (via `filter().update()` to bypass `auto_now_add` on `fecha_alta`), then forward through `0002` → `0003` and assert each row's `fecha_alta`, `fecha_baja`, `fuente`, `nota`, `creado_por`, and `desactivado_por` (NULL for legacy rows) carried verbatim. This is the regression guard for the "Critical fix #1" footgun. Both tests pass against Postgres.
- **Important #2 — race-recovery branch in `OrmMentorRepository.add_or_reactivate` had no DB-backed test; only the in-memory fake was exercising it (and the fake's emulation didn't actually exercise the same control flow).** Reviewer correct. Added `test_add_or_reactivate_recovers_from_concurrent_integrity_error` in `test_mentor_repository.py`. The test patches `MentorPeriodo.objects.create` to (a) insert a real concurrent winner via `_base_manager.create`, (b) raise `IntegrityError` once. The repo's recovery branch must then re-read the winner's row and surface `ALREADY_ACTIVE`, never propagating the error. Test passes against Postgres; the partial unique index hold (one open period) is also asserted.
- **Important #3 — `docker-compose.override.yml` shipped at the repo root would tie main to the worktree's project name and ports.** Reviewer's mechanism slightly off (Compose only auto-loads `docker-compose.override.yml` when no `-f` flag is used; the project's documented workflow always uses `-f docker-compose.dev.yml`, so the override would not be auto-merged). But the underlying conclusion is correct: the file encodes worktree-specific state (`solicitudes012`, ports 8082/8445) and shouldn't ship to main. Resolution: added `docker-compose.override.yml` to `.gitignore` along with `.worktrees/` (which had been added uncommitted to the parent repo's gitignore but not to this branch's). `git rm --cached docker-compose.override.yml` unstaged the file in this branch.
- **Suggestions** — noted but not addressed in this initiative: rename method `list` → `list_periodos` (would require contract churn elsewhere; flagged for future), friendlier "never was a mentor" empty state on detail view 404, `|title` filter producing `"Csv"` instead of `"CSV"`. Left as future polish.
- **Quality gates after fixes:** `ruff check .` clean, `mypy .` clean (332 files), full `pytest -q --ignore=tests-e2e` green at 469 tests, Tier 2 browser suite green at 4 tests.

### Plan extension — bulk deactivation (2026-04-26, mid-implementation)

- User requested two new admin actions on the mentor list: "Desactivar seleccionados" (multi-select via checkboxes) and "Desactivar todos los activos" (close every currently-open period in one shot). Confirmed via multi-choice form — answers selected: track inside 012 (extend plan), two distinct toolbar buttons (not a dropdown), best-effort partial-success semantic (skip already-closed rows, return counts).
- `plan.md` extended with a "Bulk deactivation" section: new `BulkDeactivateResult` DTO; two repo methods (`deactivate_many`, `deactivate_all_active`) implemented as single bulk Postgres `UPDATE`s filtered by `fecha_baja IS NULL`; two service methods that wrap them with count arithmetic; new `BulkDeactivateMentorsView` with two-step server-side confirm (no JS); list template gets a checkbox column + two toolbar buttons; new `confirm_bulk_deactivate.html` template.
- Why a single bulk `UPDATE` instead of looping per row: Postgres handles the UPDATE atomically; the partial unique index can't be violated (we only ever close open rows); a per-row loop would be N round trips for no audit benefit. The actor stamp is uniform (admin who hit the button), so per-row attribution is intentional.
- `status.md` extended with the new section's tasks, all unchecked.

### Code-reviewer second pass on the bulk-deactivate add — 2026-04-26

The agent flagged three Important items on the bulk-deactivation extension. All three verified, all three fixed.

- **Important #1 — `already_inactive` arithmetic conflated duplicates and unknown matrículas with truly-already-closed rows.** Reviewer correct: `bulk_deactivate(["A1","A1"], …)` for one open period yielded `closed=1, total=2, already_inactive=1`, but nothing was "already inactive" — Postgres just deduped the `IN` clause. Same defect for unknown matrículas. Fix: `service.bulk_deactivate` now de-duplicates input via `set()` before passing to the repo and computing counts. `BulkDeactivateResult.already_inactive` docstring updated to honestly say it covers both "closed before this call" and "unknown to the catalog" — the catalog cannot distinguish those post-hoc, and the flash UX doesn't need to. New service test `test_bulk_deactivate_dedupes_input_before_counting` pins the dedup behavior.
- **Important #2 — two-step confirmation could be bypassed.** Reviewer correct: any same-origin client could POST `confirmed=1` directly (CSRF stops cross-site, not same-origin), so the destructive "Desactivar todos los activos" warning page was unenforceable. Fix: replaced the `confirmed=1` flag with a server-signed token. Step 1 emits `signing.dumps({action, matriculas}, salt="mentores.bulk_deactivate")`; step 2 verifies via `signing.loads(token, salt=..., max_age=300)`. The token *carries* the action and matriculas, so a tampered second-POST cannot expand the target set after the warning page rendered. 5-minute `max_age` prevents abandoned-tab abuse. New view tests cover: tampered token rejected (`BadSignature`), expired token rejected (`SignatureExpired` mocked via `unittest.mock.patch`), token with unknown action rejected. Confirm template now carries one hidden `token` field instead of N hidden `matriculas[]` inputs — the page renders visually identical (verified via re-captured Tier 2 screenshot).
- **Important #3 — `Iterable[str]` advertised single-pass laziness that the implementations did not honor.** Reviewer correct: `OrmMentorRepository.deactivate_many` iterates `matriculas` for both the empty-check and the `IN` filter, so a generator passed in would silently break. Fix: changed both `MentorRepository.deactivate_many` and `MentorService.bulk_deactivate` signatures to `Sequence[str]`, dropped the redundant `list(matriculas)` materialization in the repo. The repo's empty-input shortcut still applies (`Sequence` supports `bool()`). Service still does `list(set(matriculas))` for the dedup pass.
- **Suggestions** noted but not addressed in this initiative: rename `<str:matricula>/` route to `<str:matricula>/historial/` (eliminates the theoretical "what if matricula is literally `agregar`?" worry; matricula validator regex makes it impossible today), input cap on `matriculas` (Postgres handles 100k entries fine; not a real risk), one-line comment on `detail.py` explaining that `is_currently_active = history[0].fecha_baja is None` relies on `get_history` newest-first ordering. Left for future polish.
- **Quality gates after fixes:** `ruff check .` clean, `mypy mentores/` clean (43 files), full `pytest -q --ignore=tests-e2e` green at 548 tests, mentor scope at 94 tests, Tier 2 mentor browser suite at 6 tests passing.

### UX revision — list-page bulk controls (2026-04-26, post-review)

User asked to simplify the list-page bulk controls: remove the per-row "Desactivar" link, collapse the two toolbar buttons into a single "Desactivar" (selection is implicit), and add a "Seleccionar todos" master toggle above the checkboxes.

- **`templates/mentores/list.html`:** dropped the "Acciones" column header + per-row Desactivar link entirely (one fewer column = better mobile reflow). Replaced the two-button toolbar with a "Seleccionar todos" `type="button"` (outline-secondary) and a single "Desactivar" `type="submit"` (outline-danger) that posts `action=selected`. Added a small inline `<script>` (functional, not decorative) that toggles every `input[type=checkbox][name=matriculas]` in the form when the master button fires — toggle behaves correctly on a second click (un-checks all). The `ACTION_ALL` server branch is intentionally retained: tested, harmless, no UI exposure today, available for a future "advanced admin" panel.
- **Bug caught + fixed during screenshot verification:** initial inline comment used `{# ... #}` Django syntax which is single-line only; the multi-line comment leaked as plain text into the rendered page. Switched to `{% comment %} ... {% endcomment %}` block syntax. Caught by reading the captured 1280x900 screenshot — DOM-only Playwright assertions would have missed it, which is exactly why the project's template-edit hook insists on visual verification.
- **`tests-e2e/test_mentor_bulk_deactivate_browser.py`:** updated `test_admin_sees_checkboxes_and_bulk_action_buttons` to assert the new button names ("Seleccionar todos" + "Desactivar") and added behavioral assertions for the master toggle (clicking once checks all 3; clicking again unchecks). The `selected_via_confirm_flow` test now scopes the "Desactivar" button lookup to the bulk form (`#bulk-deactivate-form`) since "Desactivar" is a more generic name and could collide with future page additions.
- **`tests-e2e/test_mentores_golden_path.py` (008):** the row-level "Desactivar" link click is gone, so the `test_admin_deactivates_mentor_from_list_view` flow was rewritten to use the new bulk path — tick the row's checkbox, hit toolbar Desactivar, click "Sí, desactivar seleccionados" on the confirm page. Same end-state assertions (success flash, row no longer in active filter, row visible when filter dropped). Coverage of "admin deactivates a mentor from the list view" preserved on the new path.
- **Single-row deactivate URL kept:** `/mentores/<matricula>/desactivar/` and its view + template are still wired. No UI links to it now, but the route remains available for direct admin navigation and future UI surfaces. Tier 1 view tests for `DeactivateMentorView` still pass unchanged.
- **Quality gates:** `ruff check .` clean (fixed two pre-existing RUF002 ambiguous-character warnings in the bulk-test docstring while here), `mypy mentores/` clean, full unit/integration suite at 548 tests passing, all 6 mentor browser tests passing. Fresh screenshots captured at 1280x900 + 320x800; mobile reflow verified clean.
