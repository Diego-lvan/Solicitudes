# 011 — Field Auto-fill from User Data — Changelog

> Append-only. Never edit or delete existing entries.

## 2026-04-25
- Initiative created from a conversational brainstorm during 003's closeout review.
- Key decisions:
  - **Auto-fill, not prefill.** Fields with `source != USER_INPUT` are invisible to the alumno as inputs; the backend resolves the values from the actor's `UserDTO` at submit time. Rejected: prefill-editable approach (alumno could overwrite, defeating the point of binding to canonical SIGA data).
  - **Failure is hard.** If a required auto-fill field has no resolvable value (SIGA down + cache empty), the submission fails with a clear error pointing the alumno to Control Escolar. No silent partial save. (Aligns with the user's "que falle" answer on the SIGA-down edge case.)
  - **Backend-only resolution.** The "Datos del solicitante" preview panel and the submit-time merge both call `UserService.hydrate_from_siga` server-side. Client-supplied values for auto-fill field_ids are silently dropped — the form factory excludes those fields entirely, so they cannot ride along in `form.to_values_dict()`.
  - **Source ↔ FieldType compatibility** enforced at the schema layer (`_check_source_matches_type` validator) and re-enforced at the form-clean layer (auto-resets to `USER_INPUT` on type switch).
  - **`SigaProfile` shape stays alumno-shaped for now.** Docente sources are deferred to whichever future initiative first needs one (OQ-011-2). The current `UserDTO` already returns empty values for non-applicable academic fields, so a docente filing an alumno-only tipo simply gets empty auto-fill values — no error if the field is optional.
  - **Sequencing.** Schema/admin/builder tasks depend only on 003 and can land before 004 ships intake. Resolver + intake integration depend on 004's `intake/` package existing.
- Open questions captured: OQ-011-1 (refresh button on preview panel), OQ-011-2 (docente SIGA shape), OQ-011-3 (split into 011a/011b vs single initiative).

## 2026-04-26
- Worktree `feature/011-field-autofill` created at `.worktrees/011-field-autofill` from main HEAD `0f1adc2`.
- Roadmap row flipped to **In Progress**.
- Plan migration filename corrected `0002_…` → `0005_fielddefinition_source.py` (numbers 0001–0004 already taken by 003/004/005).
- **Schema layer landed.** `FieldSource` enum + `FIELD_SOURCE_ALLOWED_TYPES` map in `tipos/constants.py`; `FieldDefinition.source` column with `USER_INPUT` default + reversible additive migration; `FieldDefinitionDTO`/`CreateFieldInput`/`FieldSnapshot` carry `source` (default `USER_INPUT`); `_check_source_matches_type` validator on `CreateFieldInput`; `OrmTipoRepository.create`/`_replace_fields`/`_to_dto` and `DefaultTipoService.snapshot` and `_collect_fields` all forward `source`.
- Tests: 6 new schema tests in `test_schemas.py` covering default, accept-on-compatible-types, reject-on-incompatible-types per source variant. `pytest solicitudes -q --ignore=solicitudes/lifecycle/tests/test_lifecycle_service.py` → **251 passed** (the lifecycle file requires `hypothesis` which is missing from the dev image — pre-existing, unrelated to 011). `manage.py makemigrations --check --dry-run` clean.
- **Admin form + UI landed.** `FieldForm.source` ChoiceField + `clean()` normalization that silently resets stale `USER_*` to `USER_INPUT` on type switch (defense in depth on top of `_check_source_matches_type` validator). New `<div class="source-cell" data-shows-for="TEXT,NUMBER">` cell in `_field_row.html` exposes the dropdown only for compatible types. `tipo_form.js` `readRowState` reads `select[name$="-source"]`; `renderField` renders an "Auto · <variant>" Bootstrap pill instead of the usual interactive control when source ≠ USER_INPUT. **Live visual verification** via MCP Playwright at 1280×900 desktop + 320×800 mobile — pill renders cleanly in the live preview pane, source dropdown only visible for TEXT/NUMBER, mobile reflows to single column. 6 new form unit tests pass; 22 form tests total green.
- **Builder change landed.** `build_django_form` filters `snapshot.fields` to `source is FieldSource.USER_INPUT` before constructing the Form class — auto-fill fields are entirely absent from `form.fields` and `field_order`, so a malicious POST `field_<auto_id>=...` cannot land in `form.to_values_dict()`. 3 new builder tests assert the exclusion + the injection-drop invariant.
- **Resolver + intake integration landed.** New `solicitudes.intake.services.auto_fill_resolver` package with abstract `AutoFillResolver` exposing two methods: `resolve` (strict — raises `AutoFillRequiredFieldMissing` when any required `USER_*` field resolves empty, used at submit time) and `preview` (lenient — never raises, returns `AutoFillPreview(items, has_missing_required)`, used by the GET handler). Single `UserService.hydrate_from_siga` round-trip per call. New `AutoFillRequiredFieldMissing` exception (subclass of `DomainValidationError` → 422). `IntakeService.get_intake_form` now returns an `IntakeFormBundle(tipo, form_cls, auto_fill)` so the view never reaches into the resolver directly. `IntakeService.create` merges `auto_values` on top of `valores` after form validation. `templates/solicitudes/intake/_solicitante_panel.html` renders the read-only "Datos del solicitante" panel with em-dash placeholders for empty values and a Bootstrap alert + disabled submit when `has_missing_required` is true. **Live visual verification** via MCP Playwright captured both happy-path (panel with resolved values, submit enabled) and missing-required (alert + em-dash + submit disabled) at desktop + mobile. 9 new resolver tests pass; full intake test suite (31 tests) updated for new signature and all green.
- **E2E + quality gates.** Tier 1 (Django Client) tests added to `test_tipo_views.py` (admin happy path + SELECT silent normalization per plan §12), `test_intake_views.py` (panel rendering, missing-required → 422, injection drop, value merge), `test_tipo_repository.py` (source round-trip on create + update). Tier 2 (Playwright) test file `tests-e2e/test_field_autofill_golden_path.py` lands for CI; manual MCP visual verification covered the same scenarios this session. Quality gates: `ruff check` → all clean. `mypy --strict` → all 14 touched files clean (5 pre-existing untyped-stub errors in `_StubLifecycleService` / `lifecycle/tests/fakes.py` are unrelated to 011 and predate this branch). `manage.py check` → clean. Final test run: **531 passed** across the full project (excluding the hypothesis-blocked `test_lifecycle_service.py` and Tier-2 Playwright that needs Chromium bootstrap).
- **Post-closeout extension: gender for gendered Spanish in PDF plantillas.** The user requested gender-driven concord ("el alumno" vs "la alumna") in PDFs. Implemented as: `User.gender = CharField(max_length=1, blank=True)` + migration `0002_user_gender`; `gender: str = ""` on `UserDTO` / `SigaProfile` / `CreateOrUpdateUserInput` with a `field_validator("gender", mode="before")` that coerces unknown codes (`"X"`, `"F"`, full words, non-strings) to `""`; `UserRepository.upsert` honours the empty-means-no-info contract for gender; `UserService.hydrate_from_siga` passes `profile.gender` through; `pdf/context.py` exposes `solicitante.genero` in both real and synthetic render contexts. Plantillas branch with `{% if solicitante.genero == "H" %}el{% elif solicitante.genero == "M" %}la{% else %}el/la{% endif %}` — no resolver / FieldSource code path needed, the admin's plantilla HTML is the only authoring surface. Created seed plantilla "Constancia con género (ejemplo)", three test alumnos (`ALUMNO_TEST` Ana M, `ALUMNO_H` Juan H, `ALUMNA_M` María M), one tipo "Constancia demo género" assigned to the plantilla, and two filed solicitudes (SOL-2026-00008 H + SOL-2026-00009 M) finalized end-to-end; both PDFs rendered in `/tmp/011-pdfs-out/` with verified text-extraction confirming correct gender concord. 11 parametrised tests on the gender coercion validator pin the regression cases.
- **Code review delta — three Important fixes applied.** Ran the `code-reviewer` agent on the full delta (incl. gender extension); three Important issues addressed: (1) `test_create_post_returns_422_when_required_auto_fill_missing` now pins `b"Control Escolar"` in the response body so the user-facing message is regression-protected; (2) `gender` is normalised at the DTO boundary via `_coerce_gender` (forgiving uppercase/strip/allowlist); (3) optional auto-fill fields with empty resolved values are now **dropped** from the resolver's output dict (matching `to_values_dict()`'s treatment of absent values) instead of persisted as `null`/`""`. Two Suggestions also addressed: a comment on `AutoFillPreview` justifying dataclass vs Pydantic, and a corrected `IntakeFormBundle` docstring noting the second SIGA hydration on POST is intentional. Final test run: **297 passed** in the touched packages, ruff clean, `manage.py check` clean.
- **SDD closeout.** Updated `requirements.md` for `intake` (RF-INT-13..17 codify auto-fill behaviour) and `usuarios` (gender field + DTO coercion contract). Updated `design.md` for `tipos` (FieldSource constants + admin UI affordances), `formularios` (FieldSnapshot.source + builder filter as security boundary), `intake` (AutoFillResolver methods + IntakeFormBundle + AutoFillRequiredFieldMissing), `usuarios` (User.gender row + DTO validator), `pdf` (solicitante.genero in render context). Updated `flows/solicitud-lifecycle.md` step 1+2 to describe the auto-fill resolution at GET (preview) and POST (strict). Roadmap row flipped to **Done**, status header flipped to **Done**.
